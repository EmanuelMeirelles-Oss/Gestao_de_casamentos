name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 12 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API para manter o Supabase ativo
        run: curl -sf https://gestao-de-casamentos.vercel.app/api/noivos
